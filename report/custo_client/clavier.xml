<?xml version="1.0" ?>
<openerp>
	<data>

		<template id="report_invoice_summary_clavier">
			<t t-set="zz" t-value="0" />
			<t t-foreach="summary_invoice_ids" t-as="summary_invoice">
				<t t-if="summary_invoice.id != invoice_id">
					<t t-set="zz" t-value="zz+1" />
				</t>
			</t>
			<t t-if="zz" >
                <xpath expr="//head" position="inside">
                    <link rel="stylesheet" href="/extraschool/static/src/css/report_invoice_summary.css"/>
                </xpath>
	        	<div id="invoice_summary" >
	        		<p style="font-size:18px;color:red;">RAPPEL 1</p><br/><br/>
	        		<h5>Madame, Monsieur, chers parents,</h5>
	        		<h5>Nous constatons que vous n’avez pas réglé votre/vos facture(s) de l’accueil extrascolaire/des repas.
                        Vous trouverez ci-dessous le détail de celle(s)-ci concernant la période mentionnée.</h5>
	        		<table>
	        			<tr>
		        			<th>Période</th>
		        			<th>Facture</th>
		        			<th>Echéance</th>
		        			<th>Montant</th>
						</tr>
		       			<t t-foreach="summary_invoice_ids.sorted(key=lambda r: r.number)" t-as="summary_invoice">
	        				<tr>
	        					<t t-if="summary_invoice.id != invoice_id">
	        					<t t-if="summary_invoice.reminder_fees == False">
	        						<tr>
				       					<td><span t-esc="'%s' % (time.strftime('%d-%m-%Y',time.strptime(summary_invoice.biller_id.period_from,'%Y-%m-%d')))" /> - <span t-esc="'%s' % (time.strftime('%d-%m-%Y',time.strptime(summary_invoice.biller_id.period_to,'%Y-%m-%d')))" /></td>
			        					<td><span t-field="summary_invoice.number" /></td>
				       					<td><span t-field="summary_invoice.payment_term" /></td>
				       					<td class="amount"><span t-esc="'%.2f €' %  (summary_invoice.balance)" /></td>

	        						</tr>
								</t>
	        					<t t-if="summary_invoice.reminder_fees == True">
	        						<t t-foreach="summary_invoice.invoice_line_ids" t-as="invoice_line">
	        							<tr>
					       					<td></td>
				        					<td><span t-field="invoice_line.description" /></td>
					       					<td><span t-field="summary_invoice.payment_term" /></td>
					       					<td class="amount"><span t-esc="TODO" /></td>
											<td class="amount"><span t-esc="'%.2f €' %  (summary_invoice.balance)" /></td>
				       					</tr>
	        						</t>
								</t>
								</t>
		       				</tr>
		        		</t>

	        		</table>
	       		</div>
			</t>
		</template>

        <template id="report_std_footer_clavier">
		    <div class="col-sm-12" style="margin-top:100px; font-size:12px;color:brown">
                <h3>heures d'ouvertures :</h3><br/>
                <span style="font-weight: bold;">Tous services</span><br/>
                du lundi au jeudi de 09h00 à 12h00<br/>
                <span style="font-weight: bold;">Services urbanisme, travaux, environnement</span> les vendredis de 15h00 à 18h00<br/>
                <span style="font-weight: bold;">Service population et état civil</span> les vendredis de 15h00 à 18h00<br/>
                <span style="font-weight: bold;">Permanence du Bourgmestre</span> le vendredi de 15h00 à 19h00<br/>
                Ces permanences sont maintenues en juillet et août mais les services pourraient être restreints durant ces périodes précises.
            </div>
		</template>

    </data>
</openerp>
